export default interface UnitOfMeasurement {
    _id?: string,
    note?: string,
    unitOfMeasurement: string,
    dataType: string,
    pattern: string,
    createdAt: string,
    updatedBy: string,
    updatedAt: string,
}