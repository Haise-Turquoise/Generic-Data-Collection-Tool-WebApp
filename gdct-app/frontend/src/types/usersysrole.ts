// THIS IS CONFUSING -- used for fetching submissions.. see submissionMenu or submissionsDashboard
export default interface UserSysRole {
    templateTypeId: string;
    appSysRole: string;
    organizationId: string;
    programId: string;
}